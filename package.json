{
  "name": "bot-discord",
  "version": "1.0.0",
  "description": "Discord AI Bot with Comet API (Claude Sonnet 4.5)",
  "main": "index.js",
  "scripts": {
    "start": "node --max-old-space-size=350 --expose-gc --gc-interval=100 index.js",
    "deploy": "node deploy-commands.js",
    "dev": "node --expose-gc index.js",
    "replit": "npm install && node --max-old-space-size=350 --expose-gc index.js",
    "vercel-build": "echo 'Build complete'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "engines": {
    "node": ">=18.x"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "@google/generative-ai": "^0.24.1",
    "axios": "^1.11.0",
    "discord.js": "^14.21.0",
    "dotenv": "^17.2.0",
    "express": "^4.21.2"
  }
}
